{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "jest --config jestconfig.json",
    "prepare": "lerna run build",
    "prepublishOnly": "npm test && lerna run lint",
    "preversion": "lerna run lint",
    "version": "lerna run format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.11.6",
    "codecov": "^3.2.0",
    "jest": "^24.5.0",
    "lerna": "^3.13.1",
    "ts-jest": "^24.0.0",
    "tslint": "^5.14.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.3.4000"
  }
}
